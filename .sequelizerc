const { resolve } = require('path');

module.exports = {
  config = revolve(__dirname, 'src', 'config', 'database.js'),
  "models-path": revolve(__dirname, 'src', 'app', 'models'),
  "migrations-path": revolve(__dirname, 'src', 'database', 'migrations'),
  "seeders-path": revolve(__dirname, 'src', 'database', 'seeds'),
};
